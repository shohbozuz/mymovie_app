import 'package:flutter/material.dart';

class Natijasi2 extends StatelessWidget {
  const Natijasi2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Fantastik Filmlar"),
      ),
      body: Center(
        child: Text("Natijasi"),
      ),
    );
  }
}
